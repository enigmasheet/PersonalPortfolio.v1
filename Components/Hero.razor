<!-- Components/Hero.razor -->
<section id="hero" class="hero-section">
    <p class="intro-text">Hi, my name is</p>
    <h1 class="hero-name">Abhay Kumar Mandal.</h1>
    <h2 class="hero-subtitle">Web Developer & Product-Oriented Technologist</h2>
    <p class="hero-description">
        I'm a results-driven developer specializing in building scalable, interactive applications using <strong>.NET</strong> and <strong>Blazor</strong>.
        With a passion for product thinking and clean architecture, I craft solutions that balance performance, maintainability, and user experience.
    </p>
    <a href="#projects">
        <button class="hero-button">View Projects</button>
    </a>
</section>

<style>
    .hero-section {
        padding: 6rem 2rem;
        text-align: center;
        max-width: 700px;
        margin: 0 auto;
    }

    .intro-text {
        font-size: 1.25rem;
        color: var(--text-secondary);
        margin-bottom: 0.5rem;
    }

    .hero-name {
        font-size: 3rem;
        font-weight: 700;
        margin: 0.25rem 0;
        color: var(--text-primary);
    }

    .hero-subtitle {
        font-size: 1.75rem;
        font-weight: 500;
        color: var(--text-accent);
        margin-bottom: 1rem;
    }

    .hero-description {
        font-size: 1.125rem;
        line-height: 1.5;
        margin-bottom: 2rem;
        color: var(--text-secondary);
    }

    .hero-button {
        padding: 0.75rem 2rem;
        font-size: 1rem;
        background-color: var(--btn-bg);
        color: var(--btn-text);
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

        .hero-button:hover {
            background-color: var(--btn-bg-hover);
        }

    /* Responsive tweak */
    @media (max-width: 600px) {
        .hero-name

    {
        font-size: 2.25rem;
    }

    .hero-subtitle {
        font-size: 1.25rem;
    }

    }</style>
