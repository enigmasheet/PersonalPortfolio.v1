<nav class="nav">
    <div class="logo"><a href="#hero">Abhay</a></div>

    <input type="checkbox" id="nav-toggle" class="nav-toggle" />
    <label for="nav-toggle" class="nav-toggle-label">
        <span></span>
    </label>

    <ul class="nav-links">
        <li><a href="#about">About</a></li>
        <li><a href="#experience">Experience</a></li>
        <li><a href="#projects">Projects</a></li>
        <li><a href="#contact">Contact</a></li>
    </ul>

    <div class="theme-toggle-desktop">
        <ThemeToggle />
    </div>
</nav>


<style>
    .nav {
        position: fixed;
        width: 100%;
        top: 0;
        background: var(--bg);
        display: flex;
        align-items: center;
        padding: 20px 40px;
        z-index: 999;
        gap: 40px;
        justify-content: space-between;
        flex-wrap: nowrap; /* better on mobile */
    }

    .logo {
        font-weight: bold;
        font-size: 1.5rem;
    }

    .nav-links {
        display: flex;
        gap: 20px;
        list-style: none;
        padding: 0;
        margin: 0;
        flex-grow: 1;
        align-items: center;
    }

        .nav-links a {
            text-decoration: none;
            color: var(--text-primary);
            font-weight: 500;
        }

    .nav-toggle {
        display: none;
    }

    .nav-toggle-label {
        display: none;
        flex-direction: column;
        cursor: pointer;
        gap: 5px;
    }

        .nav-toggle-label span,
        .nav-toggle-label span::before,
        .nav-toggle-label span::after {
            content: '';
            background: var(--text-primary);
            height: 2px;
            width: 25px;
            display: block;
            border-radius: 2px;
            position: relative;
        }

            .nav-toggle-label span::before {
                position: absolute;
                top: -8px;
            }

            .nav-toggle-label span::after {
                position: absolute;
                top: 8px;
            }

    .theme-toggle-desktop {
        flex-shrink: 0;
    }

    /* MOBILE RESPONSIVE */
    @@media (max-width: 768px) {
    .nav{
        flex-wrap: wrap;
        padding: 15px 20px;
    }

    /* Show hamburger toggle */
    .nav-toggle-label {
        display: flex;
    }

    /* Hide nav links by default */
    .nav-links {
        display: none;
        flex-direction: column;
        width: 100%;
        background: var(--bg);
        position: absolute;
        top: 100%;
        left: 0;
        padding: 15px 20px;
        gap: 15px;
        box-shadow: 0 4px 8px rgb(0 0 0 / 0.1);
        z-index: 998;
    }

    /* Show nav links when toggled */
    .nav-toggle:checked + .nav-toggle-label + .nav-links {
        display: flex;
    }

    .nav-links li {
        margin: 10px 0;
    }

    /* Hide desktop theme toggle */
    .theme-toggle-desktop {
        display: none;
    }

    /* Show theme toggle inside mobile menu */
    .nav-links::after {
        content: '';
        display: block;
        width: 100%;
        height: 1px;
        margin: 10px 0;
        background-color: var(--text-primary);
        opacity: 0.1;
    }

    /* Add a dedicated theme toggle mobile container inside nav-links */
    .nav-links li.theme-toggle-mobile {
        margin: 0;
    }

    /* If you want ThemeToggle inside mobile menu: */
    .theme-toggle-mobile {
        display: block;
        margin-top: 10px;
    }

    }
</style>
