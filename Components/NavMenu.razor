<nav class="nav">
    <div class="logo"><a href="#hero">Abhay</a></div>
    <input type="checkbox" id="nav-toggle" class="nav-toggle" />
    <label for="nav-toggle" class="nav-toggle-label">
        <span></span>
    </label>
    <ul class="nav-links">
        <li><a href="#about">About</a></li>
        <li><a href="#experience">Experience</a></li>
        <li><a href="#projects">Projects</a></li>
        <li><a href="#contact">Contact</a></li>
        <li class="theme-toggle-mobile">
            <ThemeToggle />
        </li>
    </ul>

    <!-- Theme toggle for desktop (optional) -->
    <div class="theme-toggle-desktop">
        <ThemeToggle />
    </div>
</nav>

<style>
    .nav {
        position: fixed;
        width: 100%;
        top: 0;
        background: var(--bg);
        display: flex;
        align-items: center;
        padding: 20px 40px;
        z-index: 999;
        gap: 40px;
        justify-content: space-between;
        flex-wrap: wrap;
    }

    .logo {
        font-weight: bold;
        font-size: 1.5rem;
    }

    .nav-links {
        display: flex;
        gap: 20px;
        list-style: none;
        padding: 0;
        margin: 0;
        flex-grow: 1;
        align-items: center;
    }

        .nav-links a {
            text-decoration: none;
            color: var(--text-primary);
            font-weight: 500;
        }

    /* Hide checkbox and hamburger menu by default */
    .nav-toggle {
        display: none;
    }

    .nav-toggle-label {
        display: none;
        flex-direction: column;
        cursor: pointer;
        gap: 5px;
    }

        .nav-toggle-label span,
        .nav-toggle-label span::before,
        .nav-toggle-label span::after {
            content: '';
            background: var(--text-primary);
            height: 2px;
            width: 25px;
            display: block;
            border-radius: 2px;
            position: relative;
        }

            .nav-toggle-label span::before {
                position: absolute;
                top: -8px;
            }

            .nav-toggle-label span::after {
                position: absolute;
                top: 8px;
            }

    /* Theme toggle desktop (hidden on small screens) */
    .theme-toggle-desktop {
        flex-shrink: 0;
    }

    /* Hide mobile theme toggle on desktop */
    .theme-toggle-mobile {
        display: none;
    }

    /* Responsive styles */
    @@media (max-width: 768px) {
        .nav

    {
        flex-direction: row;
        align-items: center;
    }

    .nav-links {
        display: none;
        flex-direction: column;
        width: 100%;
        background: var(--bg);
        position: absolute;
        top: 100%;
        left: 0;
        padding: 20px;
        gap: 15px;
        z-index: 998;
    }

        .nav-links li {
            margin: 10px 0;
        }

    /* Show nav links when toggled */
    .nav-toggle:checked + .nav-toggle-label + .nav-links {
        display: flex;
    }

    /* Show hamburger toggle */
    .nav-toggle-label {
        display: flex;
    }

    /* Hide desktop theme toggle on mobile */
    .theme-toggle-desktop {
        display: none;
    }

    /* Show theme toggle inside mobile menu */
    .theme-toggle-mobile {
        display: block;
    }

    }
</style>
